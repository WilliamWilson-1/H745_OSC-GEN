# H745 OSC-GEN PCB 完整接线表

本文档依据当前仓库固件的实际 GPIO 配置编写，适用于以下两块 ST 官方开发板：

- 主控板：`NUCLEO-H745ZI-Q`，板号 `MB1363`
- HMI 协处理器板：`NUCLEO-F103RB`，板号 `MB1136`
- LCD 模块：`ATK-MD0430R-800480`，4.3 英寸、800×480、40P FPC、RGB888 + GT9147 电容触摸

> 当前连接到 ST-Link 的目标被识别为 64 KiB STM32F103，固件因此使用板载 8 MHz 晶振 + `RCC_HSE_ON`。本文接线仍按先前指定的官方 `NUCLEO-F103RB` 排针编号给出；如果最终 PCB 实际换成 NUCLEO-F103RB 且由 ST-Link MCO 供时钟，应按 MB1136 板卡版本和焊桥状态另行切换为 `RCC_HSE_BYPASS`。

## 1. 排针和编号约定

### 1.1 H745 板

本文优先使用 `CN11`、`CN12` ST Morpho 排针编号：

- `CN11`：左侧 2×35、2.54 mm 排针位置。
- `CN12`：右侧 2×35、2.54 mm 排针位置。
- 奇数脚为一列，偶数脚为另一列；必须以开发板丝印的 `1` 脚、方形焊盘或原理图为准，不能凭板子上下方向猜测。
- `NUCLEO-H745ZI-Q` 的 CN11/CN12 默认只是未装配的排针焊盘。制作载板前，需要在开发板上焊接两条 2×35 排针；ST 推荐的类型为 `Samtec TSW-135-07-x-D` 或机械尺寸等效件。

### 1.2 F103 板

本文优先使用 `CN7`、`CN10` ST Morpho 排针编号：

- `CN7`：左侧 2×19、2.54 mm Morpho 排针。
- `CN10`：右侧 2×19、2.54 mm Morpho 排针。
- 奇数脚为一列，偶数脚为另一列；仍须以板上 1 脚标记为准。

### 1.3 电平规则

- 两块 MCU 的业务 I/O 都按 `3.3 V CMOS/TTL` 使用，禁止向任意 GPIO 输入 5 V。
- ATK-MD0430R 模块的 `FPC-1`、`FPC-2` 是 5 V 电源输入，但 RGB、控制和触摸信号仍是 3.3 V 电平，不能因模块使用 5 V 供电而向信号脚送 5 V。
- H745 与 F103 必须共地。
- 若两块 Nucleo 各自由 USB 供电，只连接 GND 和业务信号，**不要把两块板的 3V3、5V 输出直接并联**。
- 表内“方向”均以对应 MCU 为参照。

## 2. 一页式总接线表

| 网络名 | H745 排针脚 | H745 MCU 脚 | 方向 | 对端 | 说明 |
|---|---:|---|---|---|---|
| `OSC_IN` | CN12-62 | PF11 | 输入 | 示波器输入前端输出 | ADC1_INP2，绝对输入范围 0～3.3 V |
| `GEN_OUT` | CN12-11 | PA5 | 输出 | 波形输出缓冲/接口 | DAC1_OUT2，不能直接驱动低阻负载 |
| `ENC_A` | CN12-27 | PB4 | 输入 | 编码器 A | TIM3_CH1，固件内部上拉 |
| `ENC_B` | CN12-29 | PB5 | 输入 | 编码器 B | TIM3_CH2，固件内部上拉；保持 H745 默认 SB59 ON、SB77 OFF |
| `ENC_KEY` | CN11-61 | PE1 | 输入 | 编码器按键 | 按下接 GND，低有效，固件内部上拉 |
| `H7_TX_F1_RX` | CN12-28 | PB14 | 输出 | F103 CN10-33 / PA10 | USART1_TX → USART1_RX，115200 8N1 |
| `F1_TX_H7_RX` | CN12-26 | PB15 | 输入 | F103 CN10-21 / PA9 | USART1_RX ← USART1_TX，115200 8N1 |
| `BOARD_GND` | CN12-20 | GND | — | F103 CN10-20 / GND | 两板公共数字地；建议至少再并一根地线 |
| `LCD_BL` | CN12-15 | PA7 | 输出 | LCD FPC-34 / BL | 模块背光控制，高有效 |
| `LCD_RESET` | CN12-34 | PC4 | 输出 | LCD FPC-40 / RESET | 推荐分配；当前固件保持低，接屏前必须补充释放复位逻辑 |
| `LCD_AUX1` | CN12-6 | PC5 | 输出 | 不接 LCD FPC | 当前模块无多余控制脚，保留测试点即可 |

## 3. H745、F103 到 ATK-MD0430R-800480 的 40P FPC

### 3.1 完整端到端脚号表

以下脚序来自用户提供的《ATK-MD0430R 模块用户手册 V1.1》表 2.1.1。800×480 与 480×272 版本共用这组 40P 模块接口定义。

| FPC 脚号 | 手册名称 | PCB 网络名 | 接到 Nucleo 的脚号 | 作用与注意事项 |
|---:|---|---|---|---|
| 1 | VCC5 | `LCD_5V` | 单一 5 V 电源；可选 H745 CN11-18 | 与 FPC-2 并接，禁止接 3.3 V |
| 2 | VCC5 | `LCD_5V` | 单一 5 V 电源；可选 H745 CN11-18 | 与 FPC-1 并接；满背光按最高约 320 mA 计，电源建议留到 500 mA 以上 |
| 3 | R0 | `LCD_R0` | H745 CN11-68 / PG13 | 红色 bit 0 |
| 4 | R1 | `LCD_R1` | H745 CN12-35 / PA2 | 红色 bit 1 |
| 5 | R2 | `LCD_R2` | H745 CN11-30 / PA1 | 红色 bit 2 |
| 6 | R3 | `LCD_R3` | H745 CN11-34 / PB0 | 红色 bit 3 |
| 7 | R4 | `LCD_R4` | H745 CN12-14 / PA11 | 红色 bit 4 |
| 8 | R5 | `LCD_R5` | H745 CN11-38 / PC0 | 红色 bit 5 |
| 9 | R6 | `LCD_R6` | H745 CN12-24 / PB1 | 红色 bit 6 |
| 10 | R7 | `LCD_R7` | H745 CN12-53 / PE15 | 红色 bit 7；兼作模块 ID M0 |
| 11 | GND | `GND` | H745 CN11-20 / GND | 必须连接 |
| 12 | G0 | `LCD_G0` | H745 CN11-50 / PE5 | 绿色 bit 0 |
| 13 | G1 | `LCD_G1` | H745 CN11-62 / PE6 | 绿色 bit 1 |
| 14 | G2 | `LCD_G2` | H745 CN12-13 / PA6 | 绿色 bit 2 |
| 15 | G3 | `LCD_G3` | H745 CN12-56 / PE11 | 绿色 bit 3 |
| 16 | G4 | `LCD_G4` | H745 CN12-25 / PB10 | 绿色 bit 4 |
| 17 | G5 | `LCD_G5` | H745 CN12-18 / PB11 | 绿色 bit 5 |
| 18 | G6 | `LCD_G6` | H745 CN12-19 / PC7 | 绿色 bit 6 |
| 19 | G7 | `LCD_G7` | H745 CN12-66 / PG8 | 绿色 bit 7；兼作模块 ID M1 |
| 20 | GND | `GND` | H745 CN12-20 / GND | 必须连接 |
| 21 | B0 | `LCD_B0` | H745 CN11-48 / PE4 | 蓝色 bit 0 |
| 22 | B1 | `LCD_B1` | H745 CN12-33 / PA10 | 蓝色 bit 1 |
| 23 | B2 | `LCD_B2` | H745 CN12-1 / PC9 | 蓝色 bit 2 |
| 24 | B3 | `LCD_B3` | H745 CN12-65 / PD10 | 蓝色 bit 3 |
| 25 | B4 | `LCD_B4` | H745 CN12-49 / PE12 | 蓝色 bit 4 |
| 26 | B5 | `LCD_B5` | H745 CN12-37 / PA3 | 蓝色 bit 5 |
| 27 | B6 | `LCD_B6` | H745 CN12-3 / PB8 | 蓝色 bit 6 |
| 28 | B7 | `LCD_B7` | H745 CN12-5 / PB9 | 蓝色 bit 7；兼作模块 ID M2 |
| 29 | GND | `GND` | H745 CN12-63 / GND | 必须连接 |
| 30 | CLK | `LCD_PCLK` | H745 CN12-51 / PE14 | 像素时钟，典型 30 MHz |
| 31 | HSYNC | `LCD_HSYNC` | H745 CN12-4 / PC6 | 行同步 |
| 32 | VSYNC | `LCD_VSYNC` | H745 CN11-32 / PA4 | 场同步 |
| 33 | DE | `LCD_DE` | H745 CN12-42 / PF10 | 数据有效 |
| 34 | BL | `LCD_BL` | H745 CN12-15 / PA7 | 模块背光控制，高有效 |
| 35 | CS | `CT_RST` | F103 CN10-3 / PB8 | 手册名称虽为 CS，实际说明为电容触摸复位 |
| 36 | MOSI | `CT_SDA` | F103 CN7-21 / PB7 | GT9147 I2C SDA，3.3 V 开漏 |
| 37 | MISO | `NC` | **不连接** | 手册明确为未连接；不要接地或接 MCU |
| 38 | SCK | `CT_SCL` | F103 CN10-17 / PB6 | GT9147 I2C SCL，3.3 V 开漏 |
| 39 | PEN | `CT_INT` | F103 CN10-5 / PB9 | GT9147 触摸中断/启动地址选择 |
| 40 | RESET | `LCD_RESET` | H745 CN12-34 / PC4 | LCD 复位；推荐 10 kΩ 上拉到 3.3 V，并由固件完成复位后释放 |

> FPC-1/FPC-2 只能接一个 5 V 来源。若两块 Nucleo 都插着 USB，不要把 F103 CN7-18、H745 CN11-18 和外部 5 V 三者直接并联；载板应通过跳帽、0 Ω 选件或电源 ORing 只选择一路。

> 看 ATK-MD0430R 模块背面、40P 外部接口在右侧时，原厂 PCB 丝印标出了 `1` 和 `40`。实际制板仍应以所选 FFC/FPC 连接器数据手册的触点方向和 1 脚标记为准，尤其要区分同面触点与反面触点排线，不能只按“左到右”判断。

### 3.2 800×480 LCD 时序

当前固件的 LTDC 时序为：

| 参数 | 数值 |
|---|---:|
| 有效分辨率 | 800 × 480 |
| PCLK | 约 30 MHz |
| HSYNC | 40 像素时钟 |
| HBP | 40 像素时钟 |
| HFP | 48 像素时钟 |
| VSYNC | 1 行 |
| VBP | 32 行 |
| VFP | 13 行 |

手册给出的 800×480 典型总时序为：水平周期 928 DCLK、垂直周期 525 行；当前固件参数位于手册允许范围内。模块 ID 弱上拉/下拉编码为 `B7(M2)=1、G7(M1)=0、R7(M0)=0`。若程序需要读 ID，必须在启用 LTDC 输出前暂时把这三根线配置为输入，读取后再切回 LTDC 复用功能。

### 3.3 H745 板上必须处理的复用连接

当前 LCD 使用了 NUCLEO-H745ZI-Q 板载 Ethernet 和 USB 的复用脚。制作 PCB 不需要重做开发板电源或晶振，但以下是**信号连通性所必需**的板上配置：

| 被 LCD 占用的脚 | 原板载功能 | 使用外部 LCD 时的设置 |
|---|---|---|
| PA1 / LCD_R2 | Ethernet RMII_REF_CLK | SB80 OFF |
| PA2 / LCD_R1 | Ethernet RMII_MDIO | JP7 OFF |
| PA7 / LCD_BL | Ethernet RMII_CRS_DV | SB52 OFF |
| PC4 / LCD_RESET | Ethernet RMII_RXD0 | SB63 OFF |
| PC5 / 预留测试点 | Ethernet RMII_RXD1 | SB48 OFF；若 PC5 不引出，可不为 LCD 专门处理此焊桥 |
| PG13 / LCD_R0 | Ethernet RMII_TXD0 | SB46 OFF |
| PA10 / LCD_B1 | USB OTG FS ID | SB29 OFF |
| PA11 / LCD_R4 | USB OTG FS DM | SB26 OFF |
| PD10 / LCD_B3 | USB FS 电源使能 | JP8 OFF |

另外保持下列默认 Morpho 选择，不要改成电机控制兼容方式：

- CN12-14 连接 PA11：保持 `SB22 ON、SB10 OFF`。
- CN12-28 连接 PB14：保持 `SB37 ON、SB38 OFF`。
- CN12-29 连接 PB5：保持 `SB59 ON、SB77 OFF`。

## 4. H745 示波器输入

| 项目 | 接线 |
|---|---|
| Nucleo 输入脚 | CN12-62 / PF11 / ADC1_INP2 |
| 信号网络名 | `OSC_IN` |
| 模拟地 | CN12-32 / AGND，或就近 CN12-63 / GND |
| ADC 允许范围 | 0～3.3 V，仅单极性输入 |
| 禁止 | 负压、超过 3.3 V、市电、未限幅的感性尖峰 |

推荐 PCB 前端连接顺序：

`输入端子 → 限流电阻 → 分压/量程网络 → 偏置网络（测双极性信号时）→ 肖特基钳位/低电容保护 → 缓冲运放或 RC → CN12-62`

PCB 注意事项：

- `OSC_IN` 远离 LCD_PCLK、RGB 总线、背光开关节点和 VFD 高压区域。
- ADC 输入与 AGND 形成短回路，最好使用连续模拟地参考。
- 在 PF11 附近预留 33～100 Ω 串联电阻和 100 pF～1 nF 对地电容位置，具体值按带宽实测决定。
- 必须预留输入地端子，不能只接信号线。

## 5. H745 波形发生器输出

| 项目 | 接线 |
|---|---|
| Nucleo 输出脚 | CN12-11 / PA5 / DAC1_OUT2 |
| 信号网络名 | `GEN_OUT_RAW` |
| 信号地 | CN12-9 或 CN12-20 / GND |
| 固件输出 | 正弦波、方波、三角波，直流中心约 1.65 V |

PA5 是 MCU DAC 裸输出，不应直接接 BNC 50 Ω 终端。推荐：

`CN12-11 → 高输入阻抗轨到轨运放缓冲 → 可选增益/偏置 → 33～50 Ω 串联保护 → 输出端子`

若不加缓冲，输出端必须标注“高阻负载”，并避免大电容或低阻负载。

## 6. H745 编码器

| 编码器端 | H745 排针脚 | MCU 脚 | 接法 |
|---|---:|---|---|
| A | CN12-27 | PB4 | 编码器 A 相 |
| B | CN12-29 | PB5 | 编码器 B 相 |
| SW | CN11-61 | PE1 | 按键另一端接 GND，低有效 |
| COM | CN12-20 | GND | 编码器公共端 |

固件已对 A、B、SW 开启内部上拉。PCB 仍建议预留：

- A、B 各 100 Ω～1 kΩ 串联电阻位置。
- A、B、SW 各 10～100 nF 去抖电容位置；实际装值应根据旋转速度调试，过大会漏计脉冲。

## 7. H745 与 F103 板间 UART

| 网络名 | H745 端 | F103 端 | 方向 |
|---|---|---|---|
| `H7_TX_F1_RX` | CN12-28 / PB14 / USART1_TX | CN10-33 / PA10 / USART1_RX | H745 → F103 |
| `F1_TX_H7_RX` | CN12-26 / PB15 / USART1_RX | CN10-21 / PA9 / USART1_TX | F103 → H745 |
| `BOARD_GND_1` | CN12-20 / GND | CN10-20 / GND | 公共地 |
| `BOARD_GND_2` | CN11-20 / GND | CN7-20 / GND | 第二根公共地，推荐 |

UART 参数为 `115200 8N1`、3.3 V 电平。走线建议：

- TX/RX 各预留 22～100 Ω 源端串联电阻。
- 两根 UART 线旁至少有一根连续地回流，不要跨地平面分割。
- PB14 同时连接 H745 板载红色用户 LED 的高阻驱动支路，通常可以保留；如出现边沿或电平问题，可断开对应 LED 支路 R75。

## 8. F103 到 ATK-MD0430R / GT9147 触摸控制器

| GT9147 功能 | 模块 FPC 脚 | FPC 名称 | F103 MCU 脚 | F103 排针脚 | 方向 | 说明 |
|---|---:|---|---|---:|---|---|
| SCL | 38 | SCK | PB6 | CN10-17 | 输出/开漏 | I2C1_SCL，当前 100 kHz |
| SDA | 36 | MOSI | PB7 | CN7-21 | 双向/开漏 | I2C1_SDA |
| RESET | 35 | CS | PB8 | CN10-3 | 输出 | 低复位，启动时由固件翻转；不要按名称误接成 SPI 片选 |
| INT | 39 | PEN | PB9 | CN10-5 | 输入/启动时临时输出 | 用于地址选择和触摸中断状态 |
| GND | 11、20、29 | GND | — | CN10-20 / GND | — | 与模块、H745 共地 |

设计要求：

- ATK-MD0430R 模块由 FPC-1/FPC-2 的 5 V 输入经板载电路产生触摸所需电源，不要再把 F103 CN7-16 / 3V3 接到 FPC-1 或 FPC-2。
- SCL、SDA 的外部 2.2～4.7 kΩ 上拉位置建议保留为 DNP；先测模块上的现有上拉，确保总线上只保留一组合适阻值。
- RESET 和 INT 也建议预留 10 kΩ 上拉/下拉位置，最终按所用 GT9147 模组手册选择。
- I2C、RESET、INT 都不得被拉到 5 V。

## 9. F103 到 VFD 模块

| VFD 端 | F103 MCU 脚 | F103 排针脚 | 方向 | 建议网络名 |
|---|---|---:|---|---|
| DATA | PA0 | CN7-28 | 输出 | `VFD_DATA` |
| CLK | PA1 | CN7-30 | 输出 | `VFD_CLK` |
| CS | PA2 | CN10-35 | 输出 | `VFD_CS` |
| RESET | PA3 | CN10-37 | 输出 | `VFD_RESET` |
| EN / HON | PA4 | CN7-32 | 输出 | `VFD_EN`，高有效 |
| GND | — | CN7-19 或 CN10-20 | — | 逻辑公共地 |

### 9.1 F103 板上必须处理的 VCP 冲突

NUCLEO-F103RB 默认把 PA2/PA3 接到板载 ST-Link 虚拟串口，并可能没有把它们接通到 Morpho 排针。当前固件却把 PA2/PA3 用作 `VFD_CS` 和 `VFD_RESET`，因此必须：

- `SB13 OFF`
- `SB14 OFF`
- `SB62 ON`
- `SB63 ON`

SB13/SB14 用于断开板载 ST-Link UART，SB62/SB63 用于把目标 MCU 的 PA2/PA3 接通到 CN10-35/CN10-37。配置不正确时，不是发生总线争用，就是 VFD 控制线在排针处实际未连通。以上修改不影响 SWD 下载调试。

### 9.2 VFD 电源说明

- 上表只定义逻辑线；VFD 灯丝、高压/升压输入必须按照实际 VFD 模组规格单独设计。
- `VFD_EN` 只能接升压/高压驱动器的逻辑使能端，不能直接驱动高压负载。
- VFD 升压开关回路必须远离 `OSC_IN`、ADC 地和 GT9147 I2C，建议采用独立电源分区并在单点与数字地汇接。

## 10. 两块 Nucleo 和外设供电连接原则

用户要求不重复设计 Nucleo 板上已有电源，因此这里只列出 PCB 必须遵守的连接原则：

| 使用场景 | 推荐接法 |
|---|---|
| 两块 Nucleo 都插 USB | 不连接两板 3V3/5V；只连接 GND、UART 和业务信号 |
| 由统一外部电源供电 | 按两块板用户手册选择各自 VIN/5V/3V3 输入方式，并确保只存在一个电源源头 |
| ATK-MD0430R 模块供电 | 单一 5 V 源接 FPC-1、FPC-2；FPC-11、20、29 全部接 GND；建议 5 V 电源能力不低于 500 mA |
| LCD 与触摸 I/O | 全部按 3.3 V 电平；FPC-35/36/38/39 直接接 F103 指定脚，不接 5 V |
| LCD 背光 | 模块已带背光驱动，PA7 直接接 FPC-34 / BL 的逻辑控制端，不要另接 LED 恒流输出到 40P FPC |
| VFD 高压供电 | 独立设计并与模拟输入区域隔离；逻辑地与系统地单点连接 |

可用的便捷电源/地排针如下，但不代表必须互连：

| 板卡 | 3.3 V | 5 V | GND |
|---|---:|---:|---:|
| H745 | CN11-16 | CN11-18 | CN11-19/20/22/49/60，CN12-9/20/39/54/63 |
| F103 | CN7-16 | CN7-18 | CN7-8/19/20/22，CN10-9/20 |

## 11. PCB 布线分组与约束

### 11.1 30 MHz RGB 总线

- PCLK、HSYNC、VSYNC、DE 和 24 根 RGB 数据线应作为一个总线组，从 H745 排针到 LCD 连接器尽量短、同层、少过孔。
- PCLK 最关键，避免分叉和长测试点；建议在 H745 端预留 22～33 Ω 串联电阻。
- RGB 和同步线也建议预留 22～33 Ω 阻尼电阻阵列位置，是否装配由首板波形决定。
- 相邻高速线之间插入地回流并不是要求每根都包地，但必须保证完整连续参考平面。
- LCD 连接器附近布置足够地脚；不要让 PCLK 回流绕过地平面开槽。

### 11.2 模拟与噪声分区

- `OSC_IN` 和 `GEN_OUT_RAW` 放在模拟区。
- LCD PCLK、VFD CLK、VFD 升压电感和背光开关节点放在数字/电源噪声区。
- ADC 前端、DAC 缓冲、BNC/端子应靠近板边，避免模拟线贯穿 RGB 总线。
- 模拟地与数字地使用完整地平面时，优先通过布局控制回流，不建议随意切割地平面；VFD 高压开关回路单独围合。

## 12. 原理图/ERC 核对清单

- [ ] H745 CN11/CN12 的 1 脚方向与开发板实物一致。
- [ ] H745 CN11/CN12 已按 2×35、2.54 mm 机械尺寸设计，并考虑默认未装排针。
- [ ] F103 CN7/CN10 的 1 脚方向与开发板实物一致。
- [ ] H745 与 F103 UART 已交叉连接：TX → RX、RX ← TX。
- [ ] 两板至少连接两根 GND，且未误并联两个 USB 供电输出。
- [ ] H745 Ethernet 复用连接 SB80/JP7/SB52/SB63/SB48/SB46 已断开。
- [ ] H745 USB 复用连接 SB29/SB26/JP8 已断开。
- [ ] H745 Morpho 选择保持 PA11、PB14、PB5 默认路线。
- [ ] F103 的 SB13、SB14 已断开，SB62、SB63 已接通，使 PA2/PA3 脱离 ST-Link VCP 并连接到 CN10。
- [ ] PF11 前端确保任何情况下都限制在 0～3.3 V。
- [ ] PA5 后接高阻缓冲，未直接承受 50 Ω 负载。
- [ ] GT9147 I2C 上拉只存在一组，且上拉到 3.3 V。
- [ ] 40P 连接器方向已按 ATK 模块实物的 `1`/`40` 丝印及所选连接器触点方向核对。
- [ ] FPC-1、2 接 5 V；FPC-11、20、29 接地；FPC-37 保持不连接。
- [ ] FPC-35/36/38/39 分别接 F103 PB8/PB7/PB6/PB9，没有按 SPI 名称误接。
- [ ] FPC-40 接 H745 PC4，并已在固件中实现 LCD 复位释放；不能让 PC4 上电后永久保持低电平。
- [ ] LCD PCLK/RGB 走线短、参考地完整，串联阻尼电阻位置已预留。
- [ ] VFD 高压和背光开关区域与示波器 ADC 输入保持足够距离。

## 13. 官方依据

- [ST UM2408：STM32H7 Nucleo-144 boards (MB1363)](https://www.st.com/resource/en/user_manual/um2408-stm32h7-nucleo144-boards-mb1363-stmicroelectronics.pdf)
- [ST UM1724：STM32 Nucleo-64 boards (MB1136)](https://www.st.com/resource/en/user_manual/um1724-stm32-nucleo64-boards-mb1136-stmicroelectronics.pdf)
- 用户提供的《ATK-MD0430R 模块用户手册 V1.1》：表 1.2（5 V 供电、3.3 V I/O、800×480 版本功耗）、表 2.1.1（40P FPC 脚号）、表 2.1.2（模块 ID）、表 2.2.1.2（800×480 时序）。该手册未复制进仓库。

本文脚号同时与仓库中的 `NUCLEO_1.ioc`、`CM7/Core/Src/stm32h7xx_hal_msp.c`、`CM7/Core/Src/main.c`、`coprocessor/stm32f103_hmi/screen.ioc` 和 F103 `Core/Src/main.c` 逐项对照。
